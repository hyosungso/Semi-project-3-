<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<!-- ByCategory : 없으면 board테이블 전체,있으면 해당 카테고리에 대해서만 select 실행 -->
	<entry key="listCount">
		SELECT COUNT(*) COUNT FROM BOARD
	</entry>
	<entry key="listCountByCategory">
		SELECT COUNT(*) COUNT FROM BOARD
		WHERE CATEGORY_NO=?
	</entry>
	<entry key="selectList">
		SELECT * FROM (
		SELECT ROWNUM,S.* FROM (
		SELECT BOARD_NO,CATEGORY_NAME,BOARD_TITLE,USER_ID,COUNT,RECOMMEND,UPLOAD_DATE
		FROM BOARD
		JOIN CATEGORY USING(CATEGORY_NO)
		JOIN MEMBER ON(BOARD_WRITER=USER_NO)
		ORDER BY UPLOAD_DATE DESC) S)
		WHERE "ROWNUM" BETWEEN ? AND ?
	</entry>
	<entry key="selectListByCategory">
		SELECT * FROM (
		SELECT ROWNUM,S.* FROM (
		SELECT BOARD_NO,CATEGORY_NAME,BOARD_TITLE,USER_ID,COUNT,RECOMMEND,UPLOAD_DATE
		FROM BOARD
		JOIN CATEGORY USING(CATEGORY_NO)
		JOIN MEMBER ON(BOARD_WRITER=USER_NO)
		WHERE BOARD.CATEGORY_NO=?
		ORDER BY UPLOAD_DATE DESC) S)
		WHERE "ROWNUM" BETWEEN ? AND ?
	</entry>
</properties>
